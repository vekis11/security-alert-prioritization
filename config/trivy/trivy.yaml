# Optional Trivy config for IaC + secrets scanning
# Use with: trivy-config: config/trivy/trivy.yaml in the workflow
# See https://aquasecurity.github.io/trivy/latest/docs/configuration/ for full options

format: sarif
exit-code: 0
severity: CRITICAL,HIGH,MEDIUM
# Secret scanner config (optional - paths/patterns to include or exclude)
# secret:
#   config: config/trivy/secret.yaml
