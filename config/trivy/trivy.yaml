# Trivy config for CI pipeline (Scan CI Pipeline w/ Trivy Config)
# Use with: trivy-config: config/trivy/trivy.yaml
# See https://aquasecurity.github.io/trivy/latest/docs/configuration/

format: sarif
exit-code: 0
severity: CRITICAL,HIGH,MEDIUM
# Optional: secret-specific config
# secret:
#   config: config/trivy/secret.yaml
